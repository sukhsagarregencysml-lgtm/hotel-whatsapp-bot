name: Daily Marketing SMS Trigger

on:
  schedule:
    - cron: '30 4 * * *'
  workflow_dispatch:

jobs:
  trigger-marketing:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger 10 AM IST marketing SMS
        run: |
          curl -s "https://hotel-whatsapp-bot-2ole.onrender.com/send-marketing-now?key=hotelease2026" || true
          echo "Marketing SMS triggered at 10 AM IST"
